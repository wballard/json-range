{
  "name": "json-range",
  "version": "1.0.0",
  "description": "Generate JSON with stepped ranges of numbers, useful for when you need data to run with a template to tile",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "json-range": "bin/json-range"
  },
  "author": "Will Ballard <wballard@mailframe.net>",
  "license": "ISC",
  "dependencies": {
    "coffee-script": "^1.9.3",
    "docopt": "^0.6.2"
  }
}
